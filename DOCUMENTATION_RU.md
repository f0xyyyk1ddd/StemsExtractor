# 🎵 Stems Extractor Pro

## Полная документация и руководство пользователя

---

## 📋 Содержание

1. [Описание приложения](#описание-приложения)
2. [Возможности](#возможности)
3. [Системные требования](#системные-требования)
4. [Установка](#установка)
   - [macOS](#установка-на-macos)
   - [Windows](#установка-на-windows)
5. [Использование](#использование)
   - [Графический интерфейс (GUI)](#графический-интерфейс-gui)
   - [Командная строка (CLI)](#командная-строка-cli)
   - [Веб-интерфейс](#веб-интерфейс)
6. [Режимы разделения](#режимы-разделения)
7. [Поддерживаемые форматы](#поддерживаемые-форматы)
8. [Сборка приложения](#сборка-приложения)
9. [Устранение неполадок](#устранение-неполадок)
10. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)
11. [Информация о разработчиках](#информация-о-разработчиках)

---

## 📖 Описание приложения

**Stems Extractor Pro** — это профессиональное приложение для разделения музыкальных треков на отдельные инструментальные дорожки (стемы) с использованием технологий искусственного интеллекта и нейронных сетей.

Приложение использует библиотеку **Spleeter** от Deezer Research, которая позволяет с высокой точностью разделять аудиозаписи на составляющие части: вокал, инструментал, ударные, бас и другие элементы.

### Для кого это приложение?

- **DJ-и:** извлечение вокала или инструментала для создания ремиксов
- **Музыкальные продюсеры:** работа с отдельными элементами трека
- **Караоке-студии:** создание минусовок
- **Музыканты:** анализ и изучение отдельных партий
- **Любители музыки:** эксперименты со звуком

---

## ✨ Возможности

| Функция | Описание |
|---------|----------|
| 🎤 **Извлечение вокала** | Отделение голоса от инструментальной части |
| 🎹 **Разделение на 2 стема** | Вокал + Инструментал |
| 🥁 **Разделение на 4 стема** | Вокал + Ударные + Бас + Другое |
| 🎹 **Разделение на 5 стемов** | Вокал + Ударные + Бас + Пианино + Другое |
| 🖥️ **Графический интерфейс** | Современный темный интерфейс PyQt5 |
| 💻 **Командная строка** | Для автоматизации и скриптов |
| 🌐 **Веб-интерфейс** | Работа через браузер с FastAPI |
| 📁 **Пакетная обработка** | Обработка нескольких файлов |

---

## 💻 Системные требования

### Минимальные требования

| Компонент | Требование |
|-----------|------------|
| **ОС** | macOS 10.13+ / Windows 10+ / Linux |
| **Процессор** | Intel Core i5 или эквивалент |
| **Оперативная память** | 8 GB RAM |
| **Место на диске** | 5 GB свободного места |
| **Python** | Версия 3.8 - 3.11 |

### Рекомендуемые требования

| Компонент | Требование |
|-----------|------------|
| **Процессор** | Intel Core i7 / Apple M1+ или эквивалент |
| **Оперативная память** | 16 GB RAM |
| **GPU** | NVIDIA GPU с CUDA (опционально, для ускорения) |
| **Место на диске** | 10 GB свободного места |

### Обязательные зависимости

- **FFmpeg** — для обработки аудиофайлов
- **Python 3.8-3.11** — среда выполнения

---

## 📥 Установка

### Установка на macOS

#### Шаг 1: Установка FFmpeg

```bash
# С использованием Homebrew
brew install ffmpeg
```

#### Шаг 2: Клонирование репозитория

```bash
git clone <URL-репозитория>
cd StemsExtractor
```

#### Шаг 3: Создание виртуального окружения

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate
```

#### Шаг 4: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt

# Дополнительные зависимости для GUI
pip install PyQt5 static-ffmpeg
```

#### Шаг 5: Запуск приложения

```bash
# GUI версия
python gui.py

# Или через скрипт
chmod +x start_gui.sh
./start_gui.sh
```

---

### Установка на Windows

#### Шаг 1: Установка Python

1. Скачайте Python 3.10 с [python.org](https://www.python.org/downloads/)
2. Установите, отметив "Add Python to PATH"

#### Шаг 2: Установка FFmpeg

1. Скачайте FFmpeg с [ffmpeg.org](https://ffmpeg.org/download.html)
2. Распакуйте в папку (например, `C:\ffmpeg`)
3. Добавьте `C:\ffmpeg\bin` в переменную PATH

#### Шаг 3: Установка приложения

```cmd
# Откройте командную строку в папке проекта

# Создание виртуального окружения
python -m venv venv

# Активация
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
pip install PyQt5 static-ffmpeg
```

#### Шаг 4: Запуск

```cmd
python gui.py
```

---

## 🚀 Использование

### Графический интерфейс (GUI)

Графический интерфейс — самый простой способ использования приложения.

#### Запуск GUI

```bash
# macOS/Linux
source venv/bin/activate
python gui.py

# Или через скрипт
./start_gui.sh
```

```cmd
# Windows
venv\Scripts\activate
python gui.py
```

#### Инструкция по использованию

1. **Запустите приложение** — откроется окно с темной темой

2. **Выберите аудиофайл**
   - Нажмите кнопку **"📂 Выберите аудиофайл"**
   - В появившемся диалоге выберите нужный файл
   - Поддерживаемые форматы: MP3, WAV, FLAC, M4A, AIFF

3. **Выберите режим разделения**
   - **2 Stems** — Вокал + Инструментал (быстрее всего)
   - **4 Stems** — Вокал + Ударные + Бас + Другое
   - **5 Stems** — Добавляет отдельную дорожку пианино

4. **Нажмите "НАЧАТЬ ОБРАБОТКУ"**
   - Появится индикатор прогресса
   - Статус будет отображаться под кнопкой
   - Первый запуск может занять больше времени (загрузка моделей)

5. **Получите результат**
   - После завершения появится диалог с вопросом об открытии папки
   - Результаты сохраняются в папку `output/<имя_трека>/`
   - Каждый стем сохраняется как отдельный WAV файл

#### Кнопка "Открыть папку с результатами"

Нажмите эту кнопку в любое время, чтобы открыть папку `output` в Finder/Проводнике.

---

### Командная строка (CLI)

CLI версия идеально подходит для автоматизации и пакетной обработки.

#### Базовое использование

```bash
python cli.py "путь/к/аудио.mp3"
```

#### Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `input_file` | Путь к аудиофайлу (обязательный) | — |
| `--stems` | Количество стемов (2, 4 или 5) | 2 |
| `--output` | Папка для результатов | `output` |

#### Примеры использования

```bash
# Разделение на 2 стема (вокал + инструментал)
python cli.py "music/song.mp3"

# Разделение на 4 стема
python cli.py "music/song.mp3" --stems 4

# Разделение на 5 стемов с указанием выходной папки
python cli.py "music/song.mp3" --stems 5 --output "my_stems"

# Пакетная обработка нескольких файлов
for file in music/*.mp3; do
    python cli.py "$file" --stems 4
done
```

#### Вывод CLI

```
12:30:15 - Checking FFmpeg...
12:30:15 - Loading AI models (Spleeter)...
12:30:20 - 🎵 Separating 'song.mp3' into 4 stems...
12:30:20 - This process might take a few minutes (especially first run to download models)...
12:32:45 - ✅ Success! Output saved to: /path/to/output/song
```

---

### Веб-интерфейс

Веб-интерфейс позволяет использовать приложение через браузер.

#### Запуск веб-сервера

```bash
# Активация виртуального окружения
source venv/bin/activate

# Запуск сервера
python backend/main.py

# Или через скрипт
./run.sh
```

#### Использование

1. Откройте браузер и перейдите по адресу: **http://127.0.0.1:8000**
2. Загрузите аудиофайл через веб-форму
3. Выберите количество стемов
4. Нажмите кнопку обработки
5. Скачайте результаты

#### API Эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/` | Главная страница (интерфейс) |
| `POST` | `/api/separate` | Разделение аудио |
| `GET` | `/output/{track}/{file}` | Скачивание результата |

#### Пример API запроса (cURL)

```bash
curl -X POST "http://127.0.0.1:8000/api/separate" \
  -F "file=@song.mp3" \
  -F "stems=4"
```

---

## 🎛️ Режимы разделения

### 2 Stems (Рекомендуется для начала)

| Дорожка | Содержимое |
|---------|-----------|
| `vocals.wav` | Вокал, голос |
| `accompaniment.wav` | Инструментальная часть |

**Применение:** Создание караоке, извлечение вокала для ремиксов

---

### 4 Stems

| Дорожка | Содержимое |
|---------|-----------|
| `vocals.wav` | Вокал, голос |
| `drums.wav` | Ударные инструменты |
| `bass.wav` | Бас-гитара, синтезаторные басы |
| `other.wav` | Остальные инструменты |

**Применение:** Более глубокое редактирование, создание ремиксов, мэшапов

---

### 5 Stems

| Дорожка | Содержимое |
|---------|-----------|
| `vocals.wav` | Вокал, голос |
| `drums.wav` | Ударные инструменты |
| `bass.wav` | Бас-гитара, синтезаторные басы |
| `piano.wav` | Пианино, клавишные |
| `other.wav` | Остальные инструменты |

**Применение:** Максимальный контроль над элементами трека

---

## 📁 Поддерживаемые форматы

### Входные форматы

| Формат | Расширение | Поддержка |
|--------|------------|-----------|
| MP3 | `.mp3` | ✅ Полная |
| WAV | `.wav` | ✅ Полная |
| FLAC | `.flac` | ✅ Полная |
| M4A/AAC | `.m4a` | ✅ Полная |
| AIFF | `.aiff`, `.aif` | ✅ Полная |
| OGG | `.ogg` | ✅ Полная |

### Выходной формат

- **WAV** (44100 Hz, 16-bit) — высокое качество без потерь

---

## 🏗️ Сборка приложения

### Сборка macOS .app

#### Шаг 1: Установка PyInstaller

```bash
pip install pyinstaller
```

#### Шаг 2: Сборка

```bash
# Сборка с использованием spec файла
pyinstaller GuiApp.spec

# Создание .app бандла
python package_app.py
```

#### Шаг 3: Результат

Готовое приложение будет находиться в `dist/StemsExtractor.app`

---

### Сборка Windows .exe

#### Шаг 1: Подготовка

```cmd
pip install pyinstaller
```

#### Шаг 2: Сборка

```cmd
build_windows.bat
```

Или вручную:

```cmd
pyinstaller --name="StemsExtractor" ^
  --windowed ^
  --add-data "logo.png;." ^
  --hidden-import=spleeter ^
  --hidden-import=tensorflow ^
  --hidden-import=PyQt5 ^
  gui.py
```

---

## 🔧 Устранение неполадок

### Проблема: "FFmpeg не найден"

**Решение для macOS:**
```bash
brew install ffmpeg
```

**Решение для Windows:**
1. Скачайте FFmpeg с официального сайта
2. Добавьте путь к `ffmpeg.exe` в переменную PATH

**Альтернативное решение:**
Приложение включает `static-ffmpeg`, который должен установиться автоматически. Если нет:
```bash
pip install static-ffmpeg
```

---

### Проблема: "Ошибка загрузки моделей"

**Причина:** При первом запуске Spleeter скачивает модели (~150 MB)

**Решение:**
1. Убедитесь в наличии интернет-соединения
2. Подождите завершения загрузки (может занять несколько минут)
3. Модели сохраняются в `~/.cache/spleeter/`

---

### Проблема: "Недостаточно памяти"

**Решение:**
1. Закройте неиспользуемые приложения
2. Обрабатывайте файлы меньшей длительности
3. Используйте режим 2 стема вместо 5

---

### Проблема: "Приложение зависает при обработке"

**Причина:** Обработка аудио — ресурсоёмкий процесс

**Решение:**
1. Дождитесь завершения (трек 5 минут ≈ 1-3 минуты обработки)
2. Следите за индикатором прогресса и статусом

---

### Проблема: "TensorFlow warnings"

**Причина:** Информационные сообщения от TensorFlow

**Решение:** Эти предупреждения можно игнорировать, они не влияют на работу

---

### Проблема: "Плохое качество разделения"

**Возможные причины:**
1. Исходный файл низкого качества
2. Слишком сложная аранжировка

**Решение:**
1. Используйте исходники высокого качества (320kbps MP3, WAV, FLAC)
2. Попробуйте разные режимы стемов

---

## ❓ Часто задаваемые вопросы

### Q: Сколько времени занимает обработка?

**A:** Зависит от длины трека и мощности компьютера:
- 3-минутный трек (2 стема): 30-90 секунд
- 5-минутный трек (5 стемов): 2-5 минут
- Первый запуск дольше из-за загрузки моделей

---

### Q: Где сохраняются результаты?

**A:** В папке `output/<имя_исходного_файла>/`

Структура:
```
output/
└── my_song/
    ├── vocals.wav
    ├── drums.wav
    ├── bass.wav
    └── other.wav
```

---

### Q: Можно ли улучшить качество разделения?

**A:** Качество зависит от:
1. Качества исходника — используйте lossless форматы
2. Сложности аранжировки — некоторые треки разделяются лучше
3. Алгоритма — Spleeter обеспечивает хороший баланс скорости и качества

---

### Q: Работает ли приложение без интернета?

**A:** Да, после первого запуска. При первом запуске требуется интернет для загрузки моделей AI (~150 MB).

---

### Q: Поддерживается ли GPU ускорение?

**A:** Да, если установлен TensorFlow с поддержкой CUDA и имеется совместимая NVIDIA GPU, обработка будет автоматически ускорена.

---

### Q: Как обработать несколько файлов?

**A:** Используйте CLI версию со скриптом:

```bash
#!/bin/bash
for file in /path/to/music/*.mp3; do
    python cli.py "$file" --stems 4 --output "batch_output"
done
```

---

### Q: Почему выходные файлы в формате WAV?

**A:** WAV обеспечивает максимальное качество без потерь. Вы можете конвертировать результаты в MP3 с помощью FFmpeg:

```bash
ffmpeg -i vocals.wav -b:a 320k vocals.mp3
```

---

## 👨‍💻 Информация о разработчиках

**Stems Extractor Pro** разработан командой:

- **SCVDL** — основной разработчик
- **f0xyyy133** — соразработчик

### Слоган
> "FOR DJ'S BY DJ'S"

---

## 📄 Структура проекта

```
StemsExtractor/
├── gui.py                 # Главный файл GUI приложения
├── cli.py                 # Консольная версия
├── requirements.txt       # Python зависимости
├── logo.png               # Логотип приложения
├── icon.icns              # Иконка для macOS
│
├── backend/
│   └── main.py            # FastAPI веб-сервер
│
├── frontend/
│   ├── index.html         # Веб-интерфейс
│   ├── style.css          # Стили
│   └── script.js          # JavaScript
│
├── uploads/               # Загруженные файлы (веб-версия)
├── output/                # Результаты разделения
├── pretrained_models/     # Предзагруженные модели
│
├── run.sh                 # Скрипт запуска веб-сервера
├── start_gui.sh           # Скрипт запуска GUI
├── package_app.py         # Скрипт сборки macOS .app
├── GuiApp.spec            # Конфигурация PyInstaller
└── build_windows.bat      # Скрипт сборки для Windows
```

---

## 📝 Лицензия и благодарности

### Лицензия проекта

- **GNU General Public License v3 (GPLv3)** — лицензия Stems Extractor Pro.

### Используемые технологии

- **[Spleeter](https://github.com/deezer/spleeter)** — библиотека разделения аудио от Deezer Research (MIT License)
- **[TensorFlow](https://www.tensorflow.org/)** — платформа машинного обучения
- **[PyQt5](https://www.riverbankcomputing.com/software/pyqt/)** — фреймворк GUI
- **[FastAPI](https://fastapi.tiangolo.com/)** — веб-фреймворк
- **[FFmpeg](https://ffmpeg.org/)** — мультимедийный процессор

---

## 🔄 История версий

### Версия 1.0.0
- Первый публичный релиз
- GUI приложение с темной темой
- CLI версия для автоматизации
- Веб-интерфейс с FastAPI
- Поддержка 2, 4 и 5 стемов
- Сборка для macOS (.app) и Windows (.exe)

---

**Дата создания документации:** 31 января 2026

**Версия документации:** 1.0
